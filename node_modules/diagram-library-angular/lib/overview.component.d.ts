import { AfterViewInit, ElementRef } from '@angular/core';
import * as MindFusion from 'diagram-library';
import Diagram = MindFusion.Diagramming.Diagram;
export declare class Overview implements AfterViewInit {
    canvas: ElementRef<HTMLCanvasElement>;
    view_id: string;
    diagram: Diagram;
    enabled: boolean;
    allow_zoom: boolean;
    back_color: string;
    scale_factor: number;
    auto_s_amount: number;
    scale_mode: MindFusion.Diagramming.ScaleMode;
    ngAfterViewInit(): void;
}
